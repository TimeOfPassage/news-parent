server:
  port: 8888
spring:
  application:
    name: news-gateway
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848